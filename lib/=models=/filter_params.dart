class FilterParams {
  final String? country;
  final String? city;
  final DateTime? dateStart;
  final DateTime? dateEnd;

  const FilterParams({this.country, this.city, this.dateStart, this.dateEnd});
}
